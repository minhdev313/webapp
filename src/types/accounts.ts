export interface StudentType {
  code: string;
  email: string;
  name: string;
  phone_number: string;
  sub_major_id: number;
}

export interface LectureType {
  email: string;
  name: string;
  phone_number: string;
  sub_major_id: number;
}